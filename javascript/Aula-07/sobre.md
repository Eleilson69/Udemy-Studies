Operadores de comparação / operadores logicos: {
    operadores de comparação: {
        > : maior que;
        < : menor que;
        >= : maior ou igual;
        <= : menor ou igual;
        === : igualdade estrita;
        !== : diferente estrito;
    }

    operadores logicos: {
        $$ => and => E 
        || => OR => OU
        ! => NOT => NÃO  
    }
}